{
  "builds": [
    { "src": "backend.py", "use": "@vercel/python" },
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend.py" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
